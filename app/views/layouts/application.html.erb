<!DOCTYPE html>
<html>
  <head>
    <title>MjdmRocks</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <div class="top-bar">
      <%=image_tag("logo2.png", alt: "Rutherford Rockets Logo", id: 'logo')%>
      <h1 id="company-name">Rutherford Rocket Labs</h1>
      <% if session[:user_id] %>
        <%= form_tag session_path(session[:user_id]), method: :delete do %>
          <%= submit_tag 'Logout', id: 'logout' %>
        <% end %>
      <% end %>
    </div>

    <% if session[:user_id] %>
      <%= yield %>
    <% else %>
      <%= render 'sessions/new' %>
    <% end %>
  </body>
</html>
